@page
@model Final_project_02.Views.Specialisations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Specialisation[0].SpecName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Specialisation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpecName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SpecID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SpecID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SpecID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
